{
  "name": "mongodb-handler",
  "version": "1.0.0",
  "description": "MongoDB handler for user memory storage and retrieval",
  "main": "index.ts",
  "scripts": {
    "test": "bun test",
    "dev": "bun run --watch index.ts",
    "build": "bun build index.ts --outdir ./dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "mongodb": "^6.18.0"
  }
}