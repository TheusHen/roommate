{
  "name": "server-tests",
  "version": "1.0.0",
  "description": "Test suite for server module",
  "scripts": {
    "test": "npx jest",
    "dev": "node index.ts",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "mongodb": "^6.18.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["**/__tests__/**/*.test.ts", "**/?(*.)+(spec|test).ts"]
  }
}